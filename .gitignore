# machine/user-specific — copy config.example.json -> config.json and fill it in
config.json
# runtime state (the local watch list + per-target snapshots)
state.json
# logs / deps
server.log
node_modules/
# local design references (Claude Design handoff bundles — not shipped)
design/
# macOS cruft
.DS_Store
# local Claude Code config (launch.json etc.)
.claude/
